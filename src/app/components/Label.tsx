import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

const Label = () => {
    return (
        <OutlineWrapper>
            <div>

            </div>
        </OutlineWrapper>
    );
};

export default Label;
