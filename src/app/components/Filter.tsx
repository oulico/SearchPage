import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

export const Filter = () => {
    return (
        <OutlineWrapper>

            <div>

            </div>
        </OutlineWrapper>
    );
};
