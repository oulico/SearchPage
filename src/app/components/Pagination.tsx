import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

export const Pagination = () => {
    return (
        <OutlineWrapper>
            <div>

            </div>
        </OutlineWrapper>
    );
};
