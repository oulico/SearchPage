import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

export const Description = () => {
    return (
        <OutlineWrapper>

            <div>

            </div>
        </OutlineWrapper>
    );
};
