import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

export const Logo = () => {
    return (
        <OutlineWrapper>


            <div>

            </div>
        </OutlineWrapper>
    );
};
