import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

const Box = () => {
    return (
        <OutlineWrapper>

            <div>
            </div>
        </OutlineWrapper>
    );
};

export default Box;
