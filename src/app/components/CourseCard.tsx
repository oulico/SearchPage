import React from 'react';
import {OutlineWrapper} from "@/app/components/Outliner";

export const CourseCard = () => {
    return (
        <OutlineWrapper>
            <div>
            </div>
        </OutlineWrapper>
    );
};
