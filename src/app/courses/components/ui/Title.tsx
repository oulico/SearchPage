'use client'
import {OutlineWrapper} from "@/app/courses/components/ui/Outliner";

export const Title = () => {
    return (
        <OutlineWrapper>
            <div>
            </div>
        </OutlineWrapper>
    );
};
