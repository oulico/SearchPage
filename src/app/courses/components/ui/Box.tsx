'use client'
import {OutlineWrapper} from "@/app/courses/components/ui/Outliner";

const Box = () => {
    return (
        <OutlineWrapper>

            <div>
            </div>
        </OutlineWrapper>
    );
};

export default Box;
