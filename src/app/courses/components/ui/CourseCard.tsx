'use client'
import {OutlineWrapper} from "@/app/courses/components/ui/Outliner";

export const CourseCard = () => {
    return (
        <OutlineWrapper>
            <div>
            </div>
        </OutlineWrapper>
    );
};
