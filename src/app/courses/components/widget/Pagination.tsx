'use client'

import {OutlineWrapper} from "@/app/courses/components/ui/Outliner";

export const Pagination = () => {
    return (
        <OutlineWrapper>
            <div>

            </div>
        </OutlineWrapper>
    );
};
